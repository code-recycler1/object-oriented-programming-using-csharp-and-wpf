<Window x:Class="WPF_Start_exercises.Exercise_1_Dice"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_Start_exercises"
        mc:Ignorable="d"
        Title="Exercise 1 - Dice" Height="250" Width="400">
    <!-- Main container for the UI elements -->
    <StackPanel>
        <!-- Button to simulate rolling the dice -->
        <Button x:Name="btnRoll" 
                Content="Roll" 
                Height="40" 
                Margin="20" 
                Click="btnRoll_Click" />
        <!-- Calls the btnRoll_Click method when clicked -->

        <!-- Label to display the result of the first die -->
        <Label x:Name="lblDie1" 
               BorderBrush="Black" 
               BorderThickness="3" 
               Background="Blue" 
               Foreground="White" 
               Height="40" 
               Margin="10"
               HorizontalContentAlignment="Center" 
               VerticalContentAlignment="Center" 
               FontSize="16" 
               FontWeight="Bold"/>

        <!-- Label to display the result of the second die -->
        <Label x:Name="lblDie2" 
               BorderBrush="Black" 
               BorderThickness="3" 
               Background="Red" 
               Foreground="White" 
               Height="40" 
               Margin="10"
               HorizontalContentAlignment="Center" 
               VerticalContentAlignment="Center" 
               FontSize="16" 
               FontWeight="Bold"/>
    </StackPanel>
</Window>